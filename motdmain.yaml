---
- name: Update MOTD
  template:
    src: motd.j2
    dest: /etc/motd
    owner: root
    group: root
    mode: 0644

- name: Display MOTD update confirmation
  debug:
    msg: "MOTD updated on {{ inventory_hostname }} - Managed by {{ global.username }}"
